# Shared release artwork staging, without engine or compiler dependencies.
[package]
name = "renzora_native_build"
version = "0.1.0"
edition = "2021"
description = "Shared dependency-free release artwork staging."

[lib]
crate-type = ["rlib"]

# Deliberately empty, and it must stay that way — see the header. `xtask`
# depends on this crate precisely because it adds nothing to xtask's build.
[dependencies]

[lints]
workspace = true
