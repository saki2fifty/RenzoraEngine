# Build settings for standalone C-ABI plugins.
#
# Cargo discovers config by walking UP from the working directory and does not
# stop at a workspace root, so a plugin built in `plugins/<name>/` inherits the
# engine's `.cargo/config.toml` even though it declares `[workspace]` and is a
# separate workspace. That inheritance is actively harmful here, and this file
# exists to cut it off.
#
# Note that `rustflags` arrays are **merged** across config files, not replaced —
# the nearer file's entries are appended after the further one's. So a flag
# cannot be removed from here, only overridden by a later entry that contradicts
# it. That is why `prefer-dynamic` is switched off with an explicit `=no` rather
# than by omitting it: omitting it changes nothing at all.
#
# ## Why `prefer-dynamic` must not reach a plugin
#
# The engine sets it so `bevy_dylib` and `renzora` are shared between the
# executable, the editor bundle, and dlopen'd distribution plugins. A standalone
# plugin links none of those — but the flag also makes it link **std**
# dynamically, and Cargo names that library `std-<metadata>.dll`, where the
# metadata hashes the toolchain. The plugin's import table then names one exact
# std build.
#
# That silently destroys the entire premise. A standalone plugin is supposed to
# be buildable with any rustc on any machine precisely because it imports
# nothing from the host — the interface is passed *in* as a function table. An
# inherited `prefer-dynamic` re-introduces a toolchain-versioned import under a
# hashed filename, which is the same trap as `bevy_dylib-<metadata>` and fails
# the same way: the OS loader rejects the library before a single line of
# Renzora code runs, with no diagnostic pointing at the cause.
#
# It is easy to miss because it works fine right up until it doesn't. Build a
# plugin in this checkout with the pinned toolchain and the matching std dll is
# already staged beside the exe, so it loads. Build it with any other rustc — or
# hand the .dll to someone running a different editor release — and it stops
# loading, for a reason nothing in the plugin points to.
#
# ## Why NOT `crt-static` (it used to be here)
#
# The reasoning was that statically linking the MSVC runtime drops
# VCRUNTIME140.dll and the api-ms-win-crt-* set from the import table, leaving
# nothing but the OS. That is true and it is not worth it — measured on a
# minimal plugin, release, symbols stripped:
#
#   inherits engine `prefer-dynamic`    16 KB   imports std-<hash>.dll  ← broken
#   `prefer-dynamic=no`                113 KB   imports VCRUNTIME140 + crt
#   `prefer-dynamic=no` + `crt-static` 219 KB   imports KERNEL32 only
#
# 105 KB — about half the binary — to remove imports the host already has.
# `renzora.exe` is itself an MSVC build and imports VCRUNTIME140.dll and the
# whole api-ms-win-crt-* set, so every one of them is already loaded in the
# process before a plugin is ever mapped. The dependency a plugin "avoids" is
# one the thing loading it already took.
#
# The heap question is the only real consideration, and it settles the same way:
# without `crt-static` the plugin shares the host's CRT heap instead of getting
# its own. That is fine — and marginally safer — because the boundary never
# transfers ownership of an allocation. Plugin components are refused outright
# if they declare a destructor, and command payloads are copied by the host.

[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "prefer-dynamic=no"]

# The Unix targets keep their linker settings from the engine config (mold,
# rpath) — those are harmless for a plugin — and only switch off the dynamic
# std.
[target.x86_64-unknown-linux-gnu]
# A standalone plugin must resolve its imports without Rust symbols exported
# by the editor. Name the allocator's C runtime dependency explicitly and fail
# at link time instead of shipping a library that only some hosts can load.
rustflags = ["-C", "prefer-dynamic=no", "-C", "link-arg=-Wl,-z,defs", "-l", "c"]

[target.aarch64-apple-darwin]
rustflags = ["-C", "prefer-dynamic=no"]

[target.x86_64-apple-darwin]
rustflags = ["-C", "prefer-dynamic=no"]

# All 61 plugins share ONE target directory.
#
# Each plugin is a separate cargo project (deliberately — as workspace members
# they would inherit the engine feature unification and link Bevy, destroying
# the zero-dependency property). The cost was that every one of them rebuilt
# `syn`/`quote`/`proc-macro2`/`renzora_plugin` from scratch into its own
# `target/`: ~4s each, ~4 minutes across the set, every single build.
#
# Sharing a target dir gets the workspace's caching without workspace
# membership: the common dependencies compile once and every plugin links the
# same rlibs. Artifacts cannot collide — each package has a distinct name.
[build]
target-dir = "target"

[profile.dist]
# The precompiled alloc/core libraries may retain unused unwind references even
# with panic=abort. Cross-crate optimization removes them from no_std cdylibs;
# the Linux undefined-symbol check above prevents relying on that accidentally.
lto = "thin"
